<%- include('includes/header') %>

  <section class="max-w-7xl mx-auto my-8">
      <form 
            class="mt-8 space-y-6" 
            action="/converter/upload" 
            enctype="multipart/form-data" 
            method="post"
      >
        <input name="_csrf" value=<%= csrfToken %>  type="hidden">
        <div class="-space-y-px rounded-md shadow-sm">
          <div>
            <input name="foto" 
                   type="file"  
                   required  
                   accept='application/vnd.openxmlformats-officedocument.spreadsheetml.sheet, application/vnd.ms-excel' 
            />
            <!-- <input name="foto" type="file" /> -->
            <!-- <input 
                type="file" name="uploadfile" 
                accept='application/vnd.openxmlformats-officedocument.spreadsheetml.sheet, application/vnd.ms-excel'  
                required 
                class="relative block rounded-t-md border-0 py-1.5 text-gray-900 ring-1 
                ring-inset ring-gray-300 placeholder:text-gray-400 focus:z-10 focus:ring-2 focus:ring-inset 
                focus:ring-indigo-600 sm:text-sm sm:leading-6" 
                placeholder="enviar"
            > -->
          </div>
        </div>
        <button 
            type="submit" 
            class="group relative flex justify-center 
            rounded-md bg-indigo-600 py-2 px-3 text-sm 
            font-semibold text-white hover:bg-indigo-500 
            focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 
            focus-visible:outline-indigo-600"
        >
          Enviar
        </button>
      </form>
  </section>
  
   </body>
  
<%- include('includes/footer') %>
